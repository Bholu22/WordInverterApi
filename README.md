# WordInverterApi
Word Inverter API A .NET 9 REST API that inverts each word in a sentence and stores every request/response pair in SQL Server using Entity Framework Core. API documentation is served via Scalar UI.  Example: "abc def ghi" → "cba fed ihg"
